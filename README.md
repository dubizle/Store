# MyStore — Jumia-style E-Commerce Dashboard

A Jumia-style e-commerce front-end with:

- Product grid display
- Add to Cart functionality
- Cart panel with quantity adjust & remove
- Place Order functionality
- Orders stored in `localStorage`

### Tech Used

- HTML, CSS, JavaScript (Vanilla)
- LocalStorage for product and order persistence

### Demo

Open `index.html` in your browser to see the store in action.

### How to Contribute

1. Clone the repository
2. Open `index.html` in your browser
3. Modify `PRODUCTS` in `app.js` or add new features
